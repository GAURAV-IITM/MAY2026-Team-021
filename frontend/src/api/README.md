# API

Purpose: Holds reusable HTTP clients and API adapter setup for backend integration.

What belongs here: Axios instances, low-level request helpers, and backend client configuration.

What should never be placed here: Vue components, Pinia stores, page-level business workflows, or mock datasets.
