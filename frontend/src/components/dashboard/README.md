# Dashboard Components

Purpose: Holds reusable dashboard-focused UI components.

What belongs here: Metric cards, dashboard widgets, and visualization wrappers.

What should never be placed here: Dashboard pages, backend service calls, store definitions, or mock datasets.
