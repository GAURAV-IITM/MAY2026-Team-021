# Common Components

Purpose: Holds reusable generic UI components shared across features.

What belongs here: Modal, DataTable, Pagination, Toast, SearchBar, Breadcrumb, and empty/loading states.

What should never be placed here: Role-specific pages, backend API calls, route definitions, or feature-specific state.
