# Payment Components

Purpose: Holds reusable UI components specific to payments and receipts.

What belongs here: Payment summary widgets, receipt display components, and billing UI primitives.

What should never be placed here: Payment business logic, backend API implementations, stores, or route definitions.
