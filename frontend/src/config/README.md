# Config

Purpose: Holds application configuration values that are shared across the frontend.

What belongs here: App settings, theme configuration, language defaults, and environment-derived constants.

What should never be placed here: Business logic, component state, route guards, or API implementation details.
