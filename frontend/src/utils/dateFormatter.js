// src/utils: Framework-agnostic helpers and constants shared across the frontend.
// TODO: Add date formatting helpers after locale and display requirements are finalized.
export function formatDate(value) {
  return value
}
