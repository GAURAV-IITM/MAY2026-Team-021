// src/utils: Framework-agnostic helpers and constants shared across the frontend.
// TODO: Add route names, roles, and app constants after requirements are finalized.
export const APP_NAME = 'My Library App'

export const USER_ROLES = Object.freeze({
  ADMIN: 'admin',
  STUDENT: 'student',
  SUPER_ADMIN: 'superadmin',
})
