# Constants

Purpose: Holds shared enum-like values used by multiple frontend modules.

What belongs here: Role names, route names, permission labels, and API endpoint constants.

What should never be placed here: Mutable state, functions with side effects, service calls, or UI components.
